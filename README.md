# WodifyScraper (using Python)
Used to dowload your Attendance using Selenium and ChromBrowser

```
## Python requirements
PyVirtualDisplay
  -> pip install pyvirtualdisplay

Selenium Driver
  -> pip install -U selenium
```

```
## Others Requirements
ChromeBrowser
Selenium Server
```
